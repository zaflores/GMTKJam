using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PalmTileGenerator : MonoBehaviour
{
    /*
    [SerializeField] private GameObject tilePrefab;

    [SerializeField] private int xTiles;
    [SerializeField] private int yTiles;
	// Use this for initialization
	void Start () {
	    Vector2 offset = tilePrefab.GetComponent<SpriteRenderer>().bounds.size;
	    Vector2 startingPos = transform.position;
	    for (int i = 0; i < xTiles; i++)
	    {
	        for (int j = 0; j < yTiles; j++)
	        {
                Vector3 positionToSpawn = new Vector3(startingPos.x + offset.x*i,startingPos.y + offset.y *j,transform.position.z);
	            GameObject tempOmg = Instantiate(tilePrefab, positionToSpawn, transform.rotation);
	        }
	    }
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    */
}
